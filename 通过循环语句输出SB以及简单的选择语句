#include <stdio.h>

int main() {
    char Y;
    int line = 0;
    int SB = 0;
    while (line < 30000) {
        printf("SB: %d\n", line);
        line++;
    }
    if (line == 30000) {
        printf("cnm\n");
    }
    printf("gzy帅不帅(Y/N)\n");
    scanf(" %c", &Y); 
    if (Y == 'Y' || Y == 'y') {
        printf("thx\n");
    } else if (Y == 'N'  || Y=='n') {
        while (SB < 1000000) {
            printf("傻逼\n");
            SB++;
        }
        printf("NMSL\n");
    } else {

        printf("你智商有问题\n");
    }

    printf("thanks for your support!\n");
	return 0;
}
